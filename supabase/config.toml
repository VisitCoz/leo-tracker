# Supabase project config. `npx supabase link` ties this to the cloud project.
project_id = "tgyhsbefshvnxqxgkcqx"

[functions.ask-leo]
verify_jwt = true

# Called by the pg_cron schedule, not a logged-in user, so no JWT.
[functions.wake-watch]
verify_jwt = false
